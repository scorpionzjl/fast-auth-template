<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.4//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chachae.dao.PermissionDao">

    <select id="queryByRoleId" parameterType="integer" resultType="Permission">
        SELECT * FROM t_permission AS auth WHERE auth.id IN (SELECT id FROM t_role_permission WHERE role_id =
        #{roleId,jdbcType=INTEGER})
    </select>
</mapper>